#!/bin/bash
# 'remove' keeps /var/lib/budget-planner (your data!). Only 'purge' deletes it.
if [ "$1" = "purge" ]; then
  rm -rf /var/lib/budget-planner
  deb-systemd-helper purge budget-planner.service || true
  deb-systemd-helper unmask budget-planner.service || true
fi
